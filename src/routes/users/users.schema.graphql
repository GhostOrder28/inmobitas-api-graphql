type Query {
  users: [User]
  user(id: ID!): User
}

type Client {
  clientId: ID!
  name: String
  clientType: String
  age: String
  contactPhone: String
  clientDetails: String
}

type User {
  userId: ID!
  names: String
  email: String
  contactPhone: String
  clients: [Client]
}
